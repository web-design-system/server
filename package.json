{
  "name": "@web-design-system/server",
  "version": "0.0.0",
  "main": "./src/index.mjs",
  "scripts": {
    "build": "npm run presets && npm run tw",
    "presets": "node src/presets.mjs",
    "tw": "PRESET=default tailwindcss -i ./src/tailwind.css -o ./dist/default.css",
    "test": "echo true"
  },
  "prettier": "@cloud-cli/prettier-config",
  "repository": {
    "type": "git",
    "url": "https://github.com/web-design-system/server.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@cloud-cli/prettier-config": "^1.0.0"
  },
  "dependencies": {
    "tailwindcss": "^2.2.19"
  }
}
